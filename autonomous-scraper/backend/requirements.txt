# FastAPI and ASGI Server
fastapi==0.104.1          # Modern, fast web framework for building APIs
uvicorn[standard]==0.24.0  # ASGI server with WebSocket and HTTP/2 support

# Database and ODM
motor==3.3.2              # Async MongoDB driver for Python
pymongo==4.6.0            # MongoDB driver (required by motor)

# Data Validation and Serialization
pydantic==2.5.0           # Data validation using Python type annotations
pydantic-settings==2.1.0  # Settings management for Pydantic

# HTTP Client
httpx==0.25.2             # Async HTTP client for making requests

# Environment and Configuration
python-dotenv==1.0.0      # Load environment variables from .env files
python-multipart==0.0.6   # Support for form data parsing

# Date and Time Utilities
python-dateutil==2.8.2    # Extensions to the standard datetime module

# Logging and Monitoring
structlog==23.2.0         # Structured logging for better log analysis

# Security and Authentication (for future use)
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4    # Password hashing utilities

# CORS Support
fastapi-cors==0.0.6       # Cross-Origin Resource Sharing support

# Development and Testing Dependencies
pytest==7.4.3            # Testing framework
pytest-asyncio==0.21.1   # Async testing support
httpx==0.25.2             # HTTP client for testing (already listed above)

# Optional: Performance and Monitoring
prometheus-client==0.19.0 # Metrics collection for monitoring
psutil==5.9.6             # System and process utilities

# Optional: Rate Limiting
slowapi==0.1.9            # Rate limiting for FastAPI
redis==5.0.1              # Redis client (if using Redis for rate limiting)

# Optional: Background Tasks
celery==5.3.4             # Distributed task queue (if needed for complex workflows)

# Optional: API Documentation Enhancement
fastapi-users==12.1.2    # User authentication system (for future use)

# Utility Libraries
typing-extensions==4.8.0  # Backport of typing features
email-validator==2.1.0    # Email validation utilities
